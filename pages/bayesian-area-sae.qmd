---
title: "Area-Level Estimation (Bayesian) + Spatial"
---

This notebook extends the area-level Fay-Herriot workflow. It first reframes area-level SAE in Bayesian terms, then shows why spatial structure may be useful when neighbouring domains share unexplained patterns.

## Bayesian versus Frequentist approach

Both frequentist and Bayesian area-level SAE start from the same basic Fay-Herriot idea that the domain direct estimates are :

$$
\hat{\theta}^{DIR}_d =
x_d^\top\beta + u_d + e_d.
$$

The difference is how the unknown quantities are estimated and how uncertainty is interpreted. 

- In the **frequentist** approach, unknown quantities such as $\beta$, $\sigma_u^2$, and $u_d$ are estimated as fixed unknowns. Typical outputs are one point estimate per area (EBLUP), uncertainty measures such as RMSE, and confidence intervals.
- In the **Bayesian** approach, these unknown quantities are instead treated as uncertain and assigned prior distributions. Typical outputs are posterior means, credible intervals, and probability summaries. 

This means that, with a Bayesian approach, each area has a distribution of plausible values. This makes it natural to report a central estimate, an uncertainty interval, and probability-style summaries, such as how likely an area is to be above a certain policy threshold.

[ADD IMAGE]

Therefore, in practice, there is little need to perform Area-Level SAE using both approaches sequentially, especially when there are suitable R packages that have simplified the more complex implementation of the Bayesian approach.

## Spatial Structure

Going back to the FH model equation cited above, the non-spatial area-level model assumes the unexplained area effects $u_d$ are *independent* after accounting for covariates. In urban research, this is rarely true: Neighbouring places may share more similarities that could help inform our estimates.

**Spatial area-level models** add a structured area effect using an adjacency matrix or neighbourhood graph. This lets the model borrow strength not only from covariates, but also from nearby areas. The spatial structure is usually provided as a binary **spatial weights matrix**.

[ADD IMAGE]

## Evaluation

At this stage, it is worth comparing the evolving outputs of the area-level estimates and their uncertainties from direct estimates through each addition of covariates and spatial information. The aim is simply to check whether the extra modelling is useful and defensible.

## Code Implementation

:::{.callout-tip}
For Bayesian area-level SAE, the main package used here is [`SUMMER`](https://cran.r-project.org/package=SUMMER). It fits area-level smoothing models using INLA, and can work either from direct estimates or from survey microdata, greatly simplifying the implementation of the Bayesian approach and adding spatial structure in Area-Level SAE.

- Use [`SUMMER::smoothArea()`](https://search.r-project.org/CRAN/refmans/SUMMER/html/smoothArea.html) when you already have direct estimates and their variances. 
- Use [`SUMMER::smoothSurvey()`](https://search.r-project.org/CRAN/refmans/SUMMER/html/smoothSurvey.html) when you want `SUMMER` to calculate design-adjusted direct estimates from survey microdata before smoothing. 

The `sae` package, which applies a Frequentist approach to SAE, does not handle out-of-sample domains (i.e., domains without respondent records) and spatial structure conveniently. Instead, they must be coded manually, which diminishes its utility in many real-life scenarios.
:::

### Worked Example

Lorem Ipsum - share real example National Survey of Wales
